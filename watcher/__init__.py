"""watcher — a persistent runtime for Claude Code that watches task sources
(GitHub, Jetrix, …) and handles each event like a senior engineer.

See DESIGN.md for the full spec. This package is the multi-source engine; the
legacy single-file watcher (~/.clear-issue-watcher) keeps running until migrated.
"""
__version__ = "0.2.0"
